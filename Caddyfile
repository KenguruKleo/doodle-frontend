:80 {
    root * /usr/share/caddy
    encode gzip zstd
    
    # Вмикаємо обробку шаблонів для всіх HTML файлів
    templates
    
    # Логіка для SPA: якщо файлу немає, віддаємо index.html
    try_files {path} /index.html
    
    # Роздаємо статичні файли
    file_server
}